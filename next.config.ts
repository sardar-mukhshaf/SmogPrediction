import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["popcollectibles.store", "via.placeholder.com", "img.redbull.com", "images.remotePatterns"],

  },
};

export default nextConfig;
